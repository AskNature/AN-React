/**
* Outcome constants -- defines the action types for the "outcome" area.
*/
'use strict';

var keyMirror = require('keymirror');

var outcomeConstants = keyMirror({

  // Outcome action types
  SET_CURRENT_OUTCOME: null,

});

module.exports = outcomeConstants;
