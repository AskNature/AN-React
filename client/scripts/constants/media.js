/**
* Media constants -- defines the action types for the "strategy" area.
*/
'use strict';

var keyMirror = require('keymirror');

var itemConstants = keyMirror({

  // Strategy action types
  GET_MEDIA: null,
  GET_ALL_MEDIA: null

});

module.exports = itemConstants;
